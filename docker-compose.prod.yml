services:
  backend:
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --workers 2
    volumes: []

  web:
    environment:
      - NODE_ENV=production
    volumes: []

  nginx:
    restart: unless-stopped
